/// <reference types="vite/client" />

/** Hash corto del commit (o timestamp de fallback) con el que se compiló este build — ver vite.config.ts y src/components/VersionChecker.tsx. */
declare const __APP_VERSION__: string
